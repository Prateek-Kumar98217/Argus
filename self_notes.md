
1. Need to revise chunking strategy for text files(need accumulator logic instead of paragraph based)
2. Add "Document Context" so that the extractor can route pronouns to actual nodes, instead of creating new ones.
3. Deal with a bit of pydantic hallucinations in model output.
4. Add association table to db and redo the entrie related code.